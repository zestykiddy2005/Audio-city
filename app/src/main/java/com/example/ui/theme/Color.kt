package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Elegant Dark Palette Mapping (extracted from theme's HTML)
val DeepMidnight = Color(0xFF111318)       // bg-[#111318] (Main app dark canvas background)
val SlateDark = Color(0xFF1D1B20)          // bg-[#1D1B20] (Main card surface background)
val SurfaceDark = Color(0xFF2D2F33)        // bg-[#2D2F33] (Sub-container elements / active states)
val NeonCyan = Color(0xFFD0BCFF)           // bg-[#D0BCFF] (Primary lavender brand color / accents)
val NeonCyanMuted = Color(0x33D0BCFF)      // bg-[#D0BCFF]/20 (Muted primary accent background)
val LuxeGold = Color(0xFFFFD700)           // Classy gold highlight for special features
val VocalLavender = Color(0xFFCAC4D0)      // text-[#CAC4D0] (Secondary/subtext content)
val MutedText = Color(0xFF939094)          // text-[#939094] (Muted gray descriptors & icons)
val AccentPurple = Color(0xFF4F378B)       // bg-gradient purple start color
val GlowAmbient = Color(0xFF381E72)        // bg-[#381E72] (Dark rich purple accent)
val CardBorder = Color(0x4D49454F)         // border-[#49454F]/30 (Subtle borders)
val TextLight = Color(0xFFE6E1E5)          // text-[#E6E1E5] (Bright readable body headings)
val TextWhite = Color(0xFFE2E2E6)          // text-[#E2E2E6] (Standard content body text)
val RedFlag = Color(0xFFE46962)            // Elegant dark-red color for alerts/deletes

// Extra specific colors from the Elegant Dark design
val PlayerBackground = Color(0xFF211F26)   // bg-[#211F26] (Active playing bar container)
val ActiveNavPill = Color(0xFF4A4458)      // bg-[#4A4458] (Bottom nav bar item active pill state)
